@model WebApplication2.Models.CustomerFormViewModels
@{
    ViewBag.Title = "用户管理";
    
}

<h2>操作</h2>
@using (Html.BeginForm("Save", "Customer"))
{

    <div class="form-group">
        @Html.LabelFor(m => m.Customers.Name)
        @Html.TextBoxFor(m => m.Customers.Name, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Customers.Name)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Customers.PhoneNumber)
        @Html.TextBoxFor(m => m.Customers.PhoneNumber, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Customers.PhoneNumber)
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Customers.MembershipTypeId)
        @Html.DropDownListFor(m => m.Customers.MembershipTypeId, new SelectList(Model.MembershipTypes, "Id", "Name"), "请选择账户类型", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Customers.MembershipTypeId)
    </div>
    // 隐藏ID
    @Html.HiddenFor(m => m.Customers.Id)
    //使用Session验证是否为本站请求
    @Html.AntiForgeryToken()
    <button type="submit" class="btn btn-primary">保存</button>
}
@if (ViewBag.ErrorMessage != null)
{
    <div class="alert alert-danger">@ViewBag.ErrorMessage</div>
}